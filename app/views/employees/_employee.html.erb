<div id="<%= dom_id employee %>">
  <% if employee.profile_picture.present? %>
    <%= image_tag(employee.profile_picture, :class => "img-thumbnail") %>
  <%end%>
  <p>
  <strong>First name:</strong>
  <%= employee.first_name %>
  </p>

  <p>
  <strong>Last name:</strong>
  <%= employee.last_name %>
  </p>

</div>
